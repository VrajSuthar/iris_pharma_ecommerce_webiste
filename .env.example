# Copy to .env.local for local `vercel dev`, or set these on Vercel via
# `vercel env add` / the dashboard. Get them from your Razorpay Dashboard
# → Settings → API Keys (use the Test Mode keys while developing).
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
